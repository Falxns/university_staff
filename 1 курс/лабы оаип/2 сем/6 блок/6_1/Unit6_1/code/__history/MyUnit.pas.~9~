unit MyUnit;

interface

uses
   Vcl.Grids;

type
    Pointer = ^TText;
    TText = Record
        Data: String;
        Next: Pointer;
    end;

var
    Head, CopyHead, Buff: Pointer;
    i: Byte;

    procedure HeadCreate;
    procedure Delite(var SG: TStringGrid);

implementation

procedure HeadCreate;
begin
    i := 1;
    New(Head);
    Head^.Data := '';
    Head^.Next := nil;
    CopyHead := Head;
end;

procedure Delit(var SG: TStringGrid);
var
    ButtonSelected, j, k: ShortInt;
    Del, Prev: Pointer;
begin

end;
end.
